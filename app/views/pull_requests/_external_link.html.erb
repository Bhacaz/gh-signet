<%= link_to pull_request&.html_url,
            target: '_blank',
            class: ('link link-hover ' + (pull_request.draft ? 'text-primary' : 'text-cyan-400')) do %>
  <i class="bi bi-box-arrow-up-right"></i>
  <%= pull_request_ref_string(pull_request) %>
<% end %>
