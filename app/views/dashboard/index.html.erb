<div id="dashboard-container" class="mt-4" style="margin-bottom: 4em;">
  <% @pr_signets.each do |signet| %>
    <div class="accordion">
      <div class="accordion-item">
        <%= render partial: 'pr_list', locals: { signet: signet } %>
      </div>
    </div>
  <% end %>
</div>
